#include "main.h"

/**
 * print_alphabet - print all alphabet from a to z in lowercase
 *
 * Return: 0 if success
*/
void print_alphabet(void)
{
char i;
for (i = 'a'; i <= 'z'; i++)
_putchar(i);
_putchar ('\n');
}
