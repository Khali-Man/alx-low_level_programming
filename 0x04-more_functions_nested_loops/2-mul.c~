#include "main.h"
/**
 * mul - function that multiplies two integer
 * @a: parameter to be mulitiplied
 * @b: parameter to be multiplied
 * Return: 0 if success
*/
int mul(int a, int b)
{
mul(a);
mul(b);
}
